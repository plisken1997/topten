package org.plsk.mongo.cardsPool

import io.kotlintest.fail
import io.kotlintest.shouldBe
import io.kotlintest.specs.WordSpec

class DocumentCardsPoolRepositoryTest: WordSpec() {

  init {

    "document cards pool repository" should {

      "store a single cards pool" {
        fail("todo")
      }

      "update a single cards pool" {
        fail("todo")
      }

      "find a single cards pool" {
        fail("todo")
      }

      "fail to find a single cards pool" {
        fail("todo")
      }

    }

  }
}